DATABASE_URL="postgresql://postgres:postgres@localhost:5432/timio?schema=public"

JWT_SECRET="change-me-in-production"
# Optional: scope the auth cookie to a specific domain (e.g. ".example.com")
# for cross-subdomain sessions in production. Leave empty for local dev.
COOKIE_DOMAIN=